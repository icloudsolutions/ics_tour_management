<?xml version="1.0"?>
<odoo>
    <record id="view_order_form_package_tour" model="ir.ui.view">
        <field name="name">sale.order.form.package.tour</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="before">
                <field name="event_id" options="{'no_open': True}" placeholder="Sélectionnez un tour"/>
            </field>
        </field>
    </record>
</odoo>
